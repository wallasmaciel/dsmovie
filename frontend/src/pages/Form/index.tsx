import { Component, ReactNode } from 'react';

class Form extends Component {
    render(): ReactNode {
        return (
            <h1>Página de formulário</h1>
        );
    }
};

export default Form; 